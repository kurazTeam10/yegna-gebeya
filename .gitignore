# Firebase service account keys or any private keys (if you keep them locally)
*.json

# Firebase CLI cache or temporary files
.firebase/

# Flutter/Dart build outputs
build/
.dart_tool
.flutter-plugins
.dart_tool
.vscode/
.flutter-plugins-dependencies
.packages
.pub-cache/

# Android build files
android/.gradle/
android/app/build/
android/build/

# iOS build files
ios/Pods/
ios/Runner.xcworkspace/
ios/build/

# Firebase Crashlytics auto-generated files
crashlytics-build.properties

# Google Services config files that are auto-generated or local only
# Usually, google-services.json is checked in because it’s needed to build
# But if you have secrets or multiple envs, you might exclude some config files
# Uncomment if you keep service account keys locally:
# serviceAccountKey.json

# IntelliJ / VSCode / IDE files
.idea/
.vscode/
*.iml
